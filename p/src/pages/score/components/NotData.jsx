import React from 'react';
import UiNotData from '@/components/NotData';

import styles from '../style/NotData.scss';

const NotData = () => (
  <UiNotData className={styles.container} />
);

export default NotData;
